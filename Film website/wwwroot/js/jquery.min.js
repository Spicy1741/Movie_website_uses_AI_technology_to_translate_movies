// Global variables
let currentSection = 'home';
let isDarkTheme = false;
let favoriteMovies = [];
let recentActivity = [];
let isLoggedIn = localStorage.getItem('isLoggedIn') === 'true';
let userName = localStorage.getItem('userName') || 'Hero';
let userEmail = localStorage.getItem('userEmail') || 'hero@cinehub.com';

// Comic book elements array
const comicWords = ['POW!', 'BAM!', 'ZAP!', 'BOOM!', 'WHAM!', 'CRACK!', 'SMASH!', 'KAPOW!', 'THWACK!', 'ZOOM!'];

// Initialize comic elements
function createComicElements() {
    const container = document.getElementById('comicElements');

    // Create floating comic words
    for (let i = 0; i < 15; i++) {
        const element = document.createElement('div');
        element.className = 'pow-element';
        element.textContent = comicWords[Math.floor(Math.random() * comicWords.length)];
        element.style.left = Math.random() * 100 + '%';
        element.style.animationDelay = Math.random() * 8 + 's';
        element.style.animationDuration = (Math.random() * 4 + 6) + 's';
        container.appendChild(element);
    }

    // Create boom elements
    for (let i = 0; i < 10; i++) {
        const element = document.createElement('div');
        element.className = 'boom-element';
        element.textContent = ['BOOM!', 'BANG!', 'CRASH!'][Math.floor(Math.random() * 3)];
        element.style.left = Math.random() * 100 + '%';
        element.style.animationDelay = Math.random() * 6 + 's';
        element.style.animationDuration = (Math.random() * 3 + 4) + 's';
        container.appendChild(element);
    }

    // Create zap elements
    for (let i = 0; i < 12; i++) {
        const element = document.createElement('div');
        element.className = 'zap-element';
        element.textContent = ['ZAP!', 'SPARK!', 'FLASH!'][Math.floor(Math.random() * 3)];
        element.style.left = Math.random() * 100 + '%';
        element.style.animationDelay = Math.random() * 7 + 's';
        element.style.animationDuration = (Math.random() * 3 + 5) + 's';
        container.appendChild(element);
    }

    // Create star bursts
    for (let i = 0; i < 20; i++) {
        const star = document.createElement('div');
        star.className = 'star-burst';
        star.style.left = Math.random() * 100 + '%';
        star.style.animationDelay = Math.random() * 5 + 's';
        star.style.animationDuration = (Math.random() * 2 + 3) + 's';
        container.appendChild(star);
    }

    // Create comic bubbles
    for (let i = 0; i < 8; i++) {
        const bubble = document.createElement('div');
        bubble.className = 'comic-bubble';
        bubble.style.left = Math.random() * 100 + '%';
        bubble.style.animationDelay = Math.random() * 9 + 's';
        bubble.style.animationDuration = (Math.random() * 4 + 7) + 's';
        container.appendChild(bubble);
    }
}

// Side Navigation functionality
function toggleSideNav() {
    const sideNav = document.getElementById('sideNav');
    const overlay = document.getElementById('sideNavOverlay');

    sideNav.classList.toggle('open');
    overlay.classList.toggle('active');

    // Add comic effect
    if (sideNav.classList.contains('open')) {
        createComicBurst('💥');
    }
}

function closeSideNav() {
    const sideNav = document.getElementById('sideNav');
    const overlay = document.getElementById('sideNavOverlay');

    sideNav.classList.remove('open');
    overlay.classList.remove('active');
}

// Close side nav when clicking overlay
document.addEventListener('DOMContentLoaded', function () {
    const overlay = document.getElementById('sideNavOverlay');
    if (overlay) {
        overlay.addEventListener('click', closeSideNav);
    }
});

// Side navigation item click handlers
document.addEventListener('DOMContentLoaded', function () {
    document.querySelectorAll('.side-nav-item[data-section]').forEach(item => {
        item.addEventListener('click', (e) => {
            e.preventDefault();
            const sectionId = item.getAttribute('data-section');
            showSection(sectionId);
            closeSideNav();

            // Update active state in side nav
            document.querySelectorAll('.side-nav-item').forEach(navItem => {
                navItem.classList.remove('active');
            });
            item.classList.add('active');

            // Comic effect
            createComicBurst('⚡');
        });
    });
});

// Create comic burst effect
function createComicBurst(emoji) {
    const burst = document.createElement('div');
    burst.style.position = 'fixed';
    burst.style.top = '50%';
    burst.style.left = '50%';
    burst.style.transform = 'translate(-50%, -50%)';
    burst.style.fontSize = '3rem';
    burst.style.zIndex = '9999';
    burst.style.pointerEvents = 'none';
    burst.textContent = emoji;
    burst.className = 'comic-explosion';
    document.body.appendChild(burst);

    setTimeout(() => {
        document.body.removeChild(burst);
    }, 600);
}

// Update user interface based on login status
function updateUserInterface() {
    const profileBtn = document.getElementById('profileButton');
    const sideNavName = document.getElementById('sideNavName');
    const profileUsername = document.getElementById('profileUsername');
    const profileEmail = document.getElementById('profileEmail');
    const modalProfileName = document.getElementById('modalProfileName');

    if (isLoggedIn) {
        // Update profile button to show user avatar
        if (profileBtn) {
            profileBtn.innerHTML = '🦸';
            profileBtn.style.background = 'linear-gradient(135deg, #ff6b35 0%, #ffff00 100%)';
            profileBtn.style.color = '#000';
        }

        // Update user name displays
        if (sideNavName) sideNavName.textContent = `Welcome, ${userName}!`;
        if (profileUsername) profileUsername.textContent = userName.toUpperCase();
        if (profileEmail) profileEmail.textContent = userEmail;
        if (modalProfileName) modalProfileName.textContent = `Hello ${userName}`;
    } else {
        // Show login icon
        if (profileBtn) {
            profileBtn.innerHTML = '🔐';
            profileBtn.style.background = '';
            profileBtn.style.color = '';
        }

        // Reset to default
        if (sideNavName) sideNavName.textContent = 'Welcome, Hero!';
        if (profileUsername) profileUsername.textContent = 'HERO SUPREME';
        if (profileEmail) profileEmail.textContent = 'hero@cinehub.com';
        if (modalProfileName) modalProfileName.textContent = 'Hello Hero';
    }
}

// Navigation functionality
function goToMoviePage(movieId) {
    // Create comic effect
    createComicBurst('🎬');
    setTimeout(() => {
        window.location.href = `/Movie/Details?id=${movieId}`;
    }, 300);
}

function watchMovie(movieId) {
    createComicBurst('🍿');
    setTimeout(() => {
        window.location.href = `/Movie/Player?id=${movieId}`;
    }, 300);
}

function showSection(sectionId) {
    // Hide all sections
    document.querySelectorAll('.section').forEach(section => {
        section.classList.remove('active');
    });

    // Show target section
    const targetSection = document.getElementById(sectionId);
    if (targetSection) {
        targetSection.classList.add('active');
    }

    // Update nav links
    document.querySelectorAll('.nav-link').forEach(link => {
        link.classList.remove('active');
    });
    const targetLink = document.querySelector(`[href="#${sectionId}"]`);
    if (targetLink) {
        targetLink.classList.add('active');
    }

    currentSection = sectionId;
}

// Add event listeners to navigation links
document.addEventListener('DOMContentLoaded', function () {
    document.querySelectorAll('.nav-link').forEach(link => {
        const href = link.getAttribute('href');
        if (href && href.startsWith('#')) {
            link.addEventListener('click', (e) => {
                e.preventDefault();
                const targetSection = href.substring(1);
                showSection(targetSection);
                createComicBurst('💫');
            });
        }
    });
});

// Search functionality
function toggleSearch() {
    const searchContainer = document.getElementById('searchContainer');
    if (searchContainer) {
        searchContainer.classList.toggle('active');
        createComicBurst('🔍');
    }
}

// Theme toggle
function toggleTheme() {
    isDarkTheme = !isDarkTheme;
    document.body.classList.toggle('dark-theme', isDarkTheme);

    const themeBtn = document.querySelector('.theme-btn');
    if (themeBtn) {
        themeBtn.textContent = isDarkTheme ? '☀️' : '🌙';
    }

    // Update side nav theme text
    const themeNavItem = document.querySelector('.side-nav-item[onclick="toggleTheme()"] .side-nav-text');
    if (themeNavItem) {
        themeNavItem.textContent = isDarkTheme ? 'Light Mode' : 'Dark Mode';
    }

    createComicBurst(isDarkTheme ? '🌙' : '☀️');
}

// Profile toggle - redirect to login or show profile
function toggleProfile() {
    if (isLoggedIn) {
        // Show user profile modal if logged in
        const modal = document.getElementById('profileModal');
        if (modal) {
            modal.classList.add('active');
            createComicBurst('👤');
        }
    } else {
        // Redirect to login page if not logged in
        createComicBurst('🔐');
        setTimeout(() => {
            window.location.href = '/Account/Login';
        }, 300);
    }
}

// Profile actions
function changeAvatar() {
    createComicBurst('📸');
    alert('🦸 HERO AVATAR CHANGE ACTIVATED! 🦸');
    closeModal('profileModal');
}

function changePassword() {
    createComicBurst('🔒');
    alert('🔐 SECRET CODE CHANGE ACTIVATED! 🔐');
    closeModal('profileModal');
}

function logout() {
    isLoggedIn = false;
    userName = 'Hero';
    userEmail = 'hero@cinehub.com';
    localStorage.removeItem('isLoggedIn');
    localStorage.removeItem('userName');
    localStorage.removeItem('userEmail');

    updateUserInterface();
    closeModal('profileModal');
    createComicBurst('🚪');

    alert('🚪 HERO MODE DEACTIVATED! See you next time, hero! 🚪');
}

// Favorites functionality
function addToFavorites(button) {
    const movieCard = button.closest('.movie-card');
    const movieTitle = movieCard.querySelector('h3')?.textContent || 'Unknown Movie';

    if (button.textContent.includes('FAV')) {
        button.innerHTML = '❤️ FAV';
        button.classList.remove('favorited');
        favoriteMovies = favoriteMovies.filter(title => title !== movieTitle);
        createComicBurst('💔');

        // Remove from favorites grid
        const favoritesGrid = document.getElementById('favoritesGrid');
        if (favoritesGrid) {
            const favoriteItems = favoritesGrid.querySelectorAll('.favorite-item');
            favoriteItems.forEach(item => {
                const itemTitle = item.querySelector('.poster-placeholder')?.textContent;
                if (itemTitle === movieTitle) {
                    item.remove();
                }
            });
        }

        alert('💔 REMOVED FROM HERO COLLECTION 💔');
    }
}

// Recent activity
function showRecent() {
    const modal = document.getElementById('recentModal');
    if (modal) {
        modal.classList.add('active');
        createComicBurst('📊');

        // Populate recent movies
        const recentMoviesTable = document.getElementById('recentMoviesTable');
        if (recentMoviesTable) {
            recentMoviesTable.innerHTML = `
                <tr><td>EPIC MASTERPIECE</td><td>2h 30m</td><td>2 hours ago</td></tr>
                <tr><td>CLASSIC LEGEND</td><td>1h 45m</td><td>1 day ago</td></tr>
                <tr><td>AWARD WINNER</td><td>2h 15m</td><td>3 days ago</td></tr>
            `;
        }

        // Populate recent comments
        const recentCommentsList = document.getElementById('recentCommentsList');
        if (recentCommentsList) {
            recentCommentsList.innerHTML = `
                <div class="comment-item">
                    <div class="comment-avatar">YOU</div>
                    <div class="comment-content">ABSOLUTELY EPIC! Best superhero movie EVER! 💥💥💥</div>
                </div>
                <div class="comment-item">
                    <div class="comment-avatar">YOU</div>
                    <div class="comment-content">LEGENDARY storytelling! Can't wait for the sequel! ⚡⚡⚡</div>
                </div>
            `;
        }
    }
}

// Contact form
function submitContact(event) {
    event.preventDefault();
    createComicBurst('📞');
    alert('💥 HERO MESSAGE SENT! The League will contact you soon! 💥');
    event.target.reset();
}

// Modal close functionality
function closeModal(modalId) {
    const modal = document.getElementById(modalId);
    if (modal) {
        modal.classList.remove('active');
    }
}

// Close modals when clicking outside
document.addEventListener('DOMContentLoaded', function () {
    document.addEventListener('click', (e) => {
        if (e.target.classList.contains('modal')) {
            e.target.classList.remove('active');
        }
    });
});

// Search functionality
document.addEventListener('DOMContentLoaded', function () {
    const searchInput = document.getElementById('searchInput');
    if (searchInput) {
        searchInput.addEventListener('input', function () {
            const searchTerm = this.value.toLowerCase();
            const movieCards = document.querySelectorAll('.movie-card');

            movieCards.forEach(card => {
                const title = card.querySelector('h3')?.textContent.toLowerCase() || '';
                const placeholder = card.querySelector('.poster-placeholder')?.textContent.toLowerCase() || '';
                const isVisible = title.includes(searchTerm) || placeholder.includes(searchTerm);
                card.style.display = isVisible ? 'block' : 'none';
            });
        });
    }
});

// Filter functionality
document.addEventListener('DOMContentLoaded', function () {
    const categoryFilter = document.getElementById('categoryFilter');
    if (categoryFilter) {
        categoryFilter.addEventListener('change', function () {
            const selectedCategory = this.value;
            const movieCards = document.querySelectorAll('.movie-card[data-category]');

            movieCards.forEach(card => {
                if (!selectedCategory || card.dataset.category === selectedCategory) {
                    card.style.display = 'block';
                } else {
                    card.style.display = 'none';
                }
            });

            if (selectedCategory) {
                createComicBurst('🎯');
            }
        });
    }
});

// Add movie card click animations
document.addEventListener('DOMContentLoaded', function () {
    document.querySelectorAll('.movie-card').forEach(card => {
        card.addEventListener('mouseenter', function () {
            this.style.cursor = 'pointer';
        });

        card.addEventListener('click', function (e) {
            // Only trigger if not clicking on buttons
            if (!e.target.closest('button')) {
                this.classList.add('clicked');
                createComicBurst('💥');
                setTimeout(() => {
                    this.classList.remove('clicked');
                }, 200);
            }
        });
    });
});

// Profile functions
function editProfile() {
    createComicBurst('✏️');
    alert('🦸 HERO PROFILE EDITOR ACTIVATED! 🦸');
}

function notificationSettings() {
    createComicBurst('🔔');
    alert('🔔 HERO ALERT SETTINGS ACTIVATED! 🔔');
}

function privacySettings() {
    createComicBurst('🛡️');
    alert('🛡️ PRIVACY SHIELD ACTIVATED! 🛡️');
}

function billingSettings() {
    createComicBurst('💳');
    alert('💳 HERO BILLING PANEL ACTIVATED! 💳');
}

// Initialize page
document.addEventListener('DOMContentLoaded', function () {
    createComicElements();
    updateUserInterface();

    // Check if user just logged in
    const urlParams = new URLSearchParams(window.location.search);
    if (urlParams.get('loggedIn') === 'true') {
        createComicBurst('🎉');
        setTimeout(() => {
            createComicBurst('🦸');
        }, 500);

        // Clean URL
        window.history.replaceState({}, document.title, window.location.pathname);
    }
});

// Close side nav on window resize
window.addEventListener('resize', function () {
    if (window.innerWidth > 768) {
        closeSideNav();
    }
});

// Keyboard navigation for side nav
document.addEventListener('keydown', function (e) {
    if (e.key === 'Escape') {
        closeSideNav();
        // Close any open modals
        document.querySelectorAll('.modal').forEach(modal => {
            modal.classList.remove('active');
        });
    }
});

// Add comic sound effects (visual feedback)
function addComicSoundEffect(element, sound) {
    const soundDiv = document.createElement('div');
    soundDiv.textContent = sound;
    soundDiv.style.position = 'absolute';
    soundDiv.style.top = '-30px';
    soundDiv.style.left = '50%';
    soundDiv.style.transform = 'translateX(-50%)';
    soundDiv.style.color = '#ff6b35';
    soundDiv.style.fontWeight = 'bold';
    soundDiv.style.fontSize = '1.2rem';
    soundDiv.style.textShadow = '2px 2px 0 #000';
    soundDiv.style.animation = 'powEffect 0.5s ease-out';
    soundDiv.style.pointerEvents = 'none';
    soundDiv.style.zIndex = '1000';

    element.style.position = 'relative';
    element.appendChild(soundDiv);

    setTimeout(() => {
        if (soundDiv.parentNode) {
            soundDiv.parentNode.removeChild(soundDiv);
        }
    }, 500);
}

// Add sound effects to buttons
document.addEventListener('click', function (e) {
    if (e.target.classList.contains('btn-primary')) {
        addComicSoundEffect(e.target, 'POW!');
    } else if (e.target.classList.contains('btn-secondary')) {
        addComicSoundEffect(e.target, 'ZAP!');
    } else if (e.target.classList.contains('control-btn')) {
        addComicSoundEffect(e.target, 'BOOM!');
    } else if (e.target.classList.contains('fab')) {
        addComicSoundEffect(e.target, 'WHAM!');
    }
});

// Screen shake effect for dramatic moments
function screenShake() {
    document.body.style.animation = 'none';
    document.body.offsetHeight; // Trigger reflow
    document.body.style.animation = 'screenShake 0.5s ease-in-out';
}

// Add CSS for screen shake
const shakeStyle = document.createElement('style');
shakeStyle.textContent = `
    @keyframes screenShake {
        0%, 100% { transform: translate(0, 0) rotate(0deg); }
        10% { transform: translate(-2px, -1px) rotate(-1deg); }
        20% { transform: translate(-1px, 1px) rotate(1deg); }
        30% { transform: translate(2px, -1px) rotate(0deg); }
        40% { transform: translate(1px, 2px) rotate(1deg); }
        50% { transform: translate(-1px, 1px) rotate(-1deg); }
        60% { transform: translate(-2px, -1px) rotate(0deg); }
        70% { transform: translate(2px, -2px) rotate(-1deg); }
        80% { transform: translate(-1px, 1px) rotate(1deg); }
        90% { transform: translate(1px, -1px) rotate(0deg); }
    }
`;
document.head.appendChild(shakeStyle);

// Trigger screen shake on certain actions
function triggerEpicAction() {
    screenShake();
    createComicBurst('💥');
    setTimeout(() => createComicBurst('⚡'), 200);
    setTimeout(() => createComicBurst('🌟'), 400);
}

// Add epic action to featured movie clicks
document.addEventListener('DOMContentLoaded', function () {
    document.querySelectorAll('.movie-card.featured').forEach(card => {
        card.addEventListener('click', triggerEpicAction);
    });
});

// Add random comic elements periodically
setInterval(() => {
    if (Math.random() > 0.7) { // 30% chance every interval
        const container = document.getElementById('comicElements');
        if (container) {
            const randomWord = comicWords[Math.floor(Math.random() * comicWords.length)];

            const element = document.createElement('div');
            element.className = 'pow-element';
            element.textContent = randomWord;
            element.style.left = Math.random() * 100 + '%';
            element.style.animationDuration = '6s';
            container.appendChild(element);

            // Remove after animation
            setTimeout(() => {
                if (element.parentNode) {
                    element.parentNode.removeChild(element);
                }
            }, 6000);
        }
    }
}, 3000);

// Initialize everything when page loads
window.addEventListener('load', function () {
    // Add initial comic burst
    setTimeout(() => {
        createComicBurst('🎬');
    }, 500);

    // Start the comic atmosphere
    setTimeout(() => {
        createComicBurst('✨');
    }, 1500);
});